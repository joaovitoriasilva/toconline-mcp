# ╔══════════════════════════════════════════════════════════════════════════╗
# ║  TOC Online MCP Server — Environment Variables                           ║
# ║  Copy this file to .env and fill in the required values.                 ║
# ╚══════════════════════════════════════════════════════════════════════════╝

# ┌──────────────────────────────────────────────────────────────────────────┐
# │  REQUIRED — OAuth2 credentials                                           │
# │  Get these from TOC Online: Empresa > Configurações > Dados API          │
# └──────────────────────────────────────────────────────────────────────────┘
TOCONLINE_CLIENT_ID=
TOCONLINE_CLIENT_SECRET=

# ┌──────────────────────────────────────────────────────────────────────────┐
# │  AUTHENTICATION                                                          │
# │                                                                          │
# │  Default: run 'toconline-mcp auth' to log in via browser.                │
# │  Tokens are stored securely in the system keychain.                      │
# │                                                                          │
# │  For CI/Docker/headless environments, use one of the fallbacks below.    │
# └──────────────────────────────────────────────────────────────────────────┘

# Static Bearer token — skips OAuth entirely, no auto-refresh
#TOCONLINE_ACCESS_TOKEN=

# Refresh token — fallback when keychain is unavailable (CI/Docker)
#TOCONLINE_REFRESH_TOKEN=

# ┌──────────────────────────────────────────────────────────────────────────┐
# │  OPTIONAL — API & OAuth endpoints                                        │
# │  Defaults work for most setups. Only change if TOC Online instructs.     │
# └──────────────────────────────────────────────────────────────────────────┘

# API base URL (no trailing slash)
#TOCONLINE_BASE_URL=https://api10.toconline.pt

# OAuth2 token endpoint (the /auth endpoint is derived automatically)
#TOCONLINE_OAUTH_TOKEN_URL=https://app10.toconline.pt/oauth/token

# OAuth2 redirect URI (only change for custom setups)
#TOCONLINE_REDIRECT_URI=https://oauth.pstmn.io/v1/callback

# ┌──────────────────────────────────────────────────────────────────────────┐
# │  OPTIONAL — Safety & module controls                                     │
# └──────────────────────────────────────────────────────────────────────────┘

# Block all write operations (create, update, delete)
#TOCONLINE_READ_ONLY=false

# Max write tool calls per MCP session (prevents runaway LLM loops, 0 = unlimited)
#TOCONLINE_MAX_WRITE_CALLS_PER_SESSION=50

# Load only specific tool modules (comma-separated)
# Available: customers, suppliers, addresses, contacts, products, services,
#            sales_documents, sales_receipts, purchase_documents,
#            purchase_payments, auxiliary
#TOCONLINE_MODULES=customers,suppliers,sales_documents